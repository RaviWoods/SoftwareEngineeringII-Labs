#ifndef INTVECTOR_HPP 
#define INTVECTOR_HPP

class intvector{
	public:
		intvector();
		intvector(int size);
		~intvector();
		
		void push_back(int n);
		int size() const;
		int capacity() const;
	private:
		int* v;
		int d_size;
		int d_capacity; 
}
